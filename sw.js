self.addEventListener('push',event=>{let d={};try{d=event.data?event.data.json():{}}catch(_){}event.waitUntil(self.registration.showNotification(d.title||'Controle de Estoque',{body:d.body||'Nova atualização no estoque.',tag:d.tag||'estoque-notificacao',data:d.data||{},requireInteraction:true,actions:[{action:'confirmar',title:'Confirmar'}]}))});
self.addEventListener('notificationclick',event=>{const notification=event.notification;const action=event.action;event.waitUntil((async()=>{if(action==='confirmar'){notification.close();return}const list=await clients.matchAll({type:'window',includeUncontrolled:true});for(const c of list){if('focus'in c){await c.focus();return}}await clients.openWindow('./')})())});
