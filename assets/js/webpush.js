const WEBPUSH_API='https://lozqdrqujouvjfflgasl.supabase.co/functions/v1/mg2-workplace';
function pushB64ToUint8Array(base64){const pad='='.repeat((4-base64.length%4)%4);const raw=atob((base64+pad).replace(/-/g,'+').replace(/_/g,'/'));return Uint8Array.from([...raw].map(c=>c.charCodeAt(0)))}
function getConfirmedNotifications(){try{return JSON.parse(localStorage.getItem('push-confirmed-notifications')||'[]')}catch(e){return[]}}
function confirmNotification(id){const ids=getConfirmedNotifications();if(!ids.includes(String(id))){ids.push(String(id));localStorage.setItem('push-confirmed-notifications',JSON.stringify(ids))}renderTechnicianNotifications()}
async function renderTechnicianNotifications(){const area=document.getElementById('technicianNotificationArea');const techId=localStorage.getItem('push-tecnico-id');if(!area||!techId)return;try{const r=await fetch(`${WEBPUSH_API}?action=stock&workspace=pdp`);const d=await r.json();const confirmed=getConfirmedNotifications();const pending=(d.data||[]).filter(x=>String(x.tecnico_contato_id||'')===String(techId)&&x.contatar_tecnico===true&&!confirmed.includes(String(x.id)));if(!pending.length){area.innerHTML='';return}area.innerHTML=pending.map(x=>`<div class="technicianNotification card"><div><strong>🔔 Equipamento disponível para retirada</strong><div class="muted">Chamado: <b>${esc(x.chamado)}</b> • Ativo: <b>${esc(x.ativo)}</b> • Colaborador: ${esc(x.colaborador)}</div></div><button type="button" class="blue" data-confirm-notification="${esc(x.id)}">✓ Confirmar</button></div>`).join('');area.querySelectorAll('[data-confirm-notification]').forEach(b=>b.addEventListener('click',()=>confirmNotification(b.dataset.confirmNotification)))}catch(e){console.error('Notificações pendentes:',e)}}
async function setupWebPush(){
  if(!('serviceWorker' in navigator)||!('PushManager' in window)||!('Notification' in window))return;
  try{const reg=await navigator.serviceWorker.register('./sw.js',{scope:'./'});const r=await fetch(`${WEBPUSH_API}?action=push-config`);const cfg=await r.json();if(!cfg.publicKey)return;let permission=Notification.permission;if(permission==='default')permission=await Notification.requestPermission();if(permission!=='granted')return;let sub=await reg.pushManager.getSubscription();if(!sub)sub=await reg.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:pushB64ToUint8Array(cfg.publicKey)});const techId=localStorage.getItem('push-tecnico-id');if(!techId)return;await fetch(WEBPUSH_API,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'subscribe_push',tecnico_id:techId,subscription:sub.toJSON()})});await renderTechnicianNotifications();
  }catch(e){console.error('Web Push:',e)}
}
async function choosePushTechnician(){
  try{const r=await fetch(`${WEBPUSH_API}?action=technicians`);const d=await r.json();const list=d.data||[];const modal=document.getElementById('pushTechnicianModal');const select=document.getElementById('pushTechnicianSelect');if(!modal||!select)return;select.innerHTML='<option value="">Selecione seu nome</option>'+list.map(t=>`<option value="${t.id}">${esc(t.nome)}</option>`).join('');const saved=localStorage.getItem('push-tecnico-id');if(saved&&list.some(t=>String(t.id)===String(saved))){modal.classList.add('hidden');await setupWebPush();return}modal.classList.remove('hidden');document.getElementById('pushTechnicianConfirm').onclick=async()=>{const id=select.value;if(!id){document.getElementById('pushTechnicianMsg').textContent='Selecione seu nome.';return}localStorage.setItem('push-tecnico-id',id);modal.classList.add('hidden');await setupWebPush()};
  }catch(e){console.error('Técnicos:',e)}
}
document.addEventListener('DOMContentLoaded',()=>setTimeout(choosePushTechnician,150));